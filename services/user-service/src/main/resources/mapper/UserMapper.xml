<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudshop.user.mapper.UserMapper">

    <resultMap id="UserResult" type="com.cloudshop.user.domain.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="password_hash" property="passwordHash" />
        <result column="salt" property="salt" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, phone, password_hash, salt, role, status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.cloudshop.user.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone, password_hash, salt, role, status)
        VALUES (#{username}, #{email}, #{phone}, #{passwordHash}, #{salt}, #{role}, #{status})
    </insert>

    <select id="findByUsername" parameterType="string" resultMap="UserResult">
        SELECT <include refid="Base_Column_List" /> FROM users WHERE username = #{username} LIMIT 1
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserResult">
        SELECT <include refid="Base_Column_List" /> FROM users WHERE email = #{email} LIMIT 1
    </select>

    <select id="findByPhone" parameterType="string" resultMap="UserResult">
        SELECT <include refid="Base_Column_List" /> FROM users WHERE phone = #{phone} LIMIT 1
    </select>

    <update id="updateById" parameterType="com.cloudshop.user.domain.User">
        UPDATE users 
        SET username = #{username},
            email = #{email},
            phone = #{phone},
            password_hash = #{passwordHash},
            salt = #{salt},
            role = #{role},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
