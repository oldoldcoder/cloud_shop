<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudshop.user.mapper.LoginLogMapper">
    <insert id="insert" parameterType="com.cloudshop.user.domain.LoginLog">
        INSERT INTO login_logs (user_id, login_time, ip_address, user_agent, status, message)
        VALUES (#{userId}, NOW(), #{ipAddress}, #{userAgent}, #{status}, #{message})
    </insert>
</mapper>
